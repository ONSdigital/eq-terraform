{
  "username": "${username}",
  "password": "${password}",
  "engine": "${engine}",
  "host": "${host}",
  "port": ${port},
  "dbname": "${dbname}",
  "dbInstanceIdentifier": "${dbInstanceIdentifier}"
}